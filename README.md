Installation
------------
```bash
git clone git@github.com:nijotz/nomad.git ~/.nomad
~/.nomad/nomad
```

TODO
----
* Configure iterm2 to store/load config in dotfiles
* Subcommands
* Colors
* White-label
* Link dotfiles into config if they are in a config folder in dotfiles
* Lots of if [[ -z $binary ]]; make dry
* Support dynamic configs
  * git config --global core.editor $(which nvim)
* Support OS-dynamic shell config
  * gdate/gsed for OSX
